import { describe, it } from 'vitest';

describe('HistogramChart', () => {
  it.todo('renders histogram bins for numeric data');

  // Wave 0 stub — will be unskipped in Task 2 when HistogramChart component is created
  it.skip('renders without crashing given numeric column data', async () => {
    // Placeholder: component created in Task 2
  });
});
