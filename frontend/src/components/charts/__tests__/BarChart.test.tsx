import { describe, it } from 'vitest';

describe('BarChartComponent', () => {
  it.todo('renders top 20 categories');

  // Wave 0 stub — will be unskipped in Task 2 when BarChartComponent is created
  it.skip('renders without crashing given categorical column data', async () => {
    // Placeholder: component created in Task 2
  });
});
