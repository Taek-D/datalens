---
phase: 03-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/analysis/SummaryCard.tsx
  - frontend/src/components/analysis/ColumnStatsTable.tsx
  - frontend/src/components/analysis/QualityAlerts.tsx
  - frontend/src/components/analysis/__tests__/SummaryCard.test.tsx
  - frontend/src/components/analysis/__tests__/QualityAlerts.test.tsx
  - backend/app/api/scatter.py
  - backend/main.py
  - backend/tests/test_scatter.py
autonomous: true
requirements:
  - SUMM-01
  - SUMM-02
  - SUMM-03
  - SUMM-04
  - VIZL-07

must_haves:
  truths:
    - "Summary card displays row count, column count, missing ratio, and duplicate count in a 4-column grid"
    - "Per-column numeric stats (mean, std, min, max, Q1, median, Q3, skewness) are displayed"
    - "Missing ratio is shown in the summary card (no separate chart needed per user decision)"
    - "Quality alerts show severity-colored banners with column name and message"
    - "Scatter endpoint returns at most 2000 downsampled points for a given column pair"
  artifacts:
    - path: "frontend/src/components/analysis/SummaryCard.tsx"
      provides: "4-stat grid dashboard card"
      exports: ["SummaryCard"]
    - path: "frontend/src/components/analysis/ColumnStatsTable.tsx"
      provides: "Per-column numeric statistics table"
      exports: ["ColumnStatsTable"]
    - path: "frontend/src/components/analysis/QualityAlerts.tsx"
      provides: "Severity-colored alert banners"
      exports: ["QualityAlerts"]
    - path: "frontend/src/components/analysis/__tests__/SummaryCard.test.tsx"
      provides: "Render unit tests for SummaryCard (SUMM-01)"
    - path: "frontend/src/components/analysis/__tests__/QualityAlerts.test.tsx"
      provides: "Render unit tests for QualityAlerts (SUMM-04)"
    - path: "backend/app/api/scatter.py"
      provides: "POST /api/scatter endpoint with 2000-point downsampling"
      exports: ["router"]
    - path: "backend/tests/test_scatter.py"
      provides: "Pytest tests for scatter endpoint"
  key_links:
    - from: "frontend/src/components/analysis/SummaryCard.tsx"
      to: "AnalysisResultResponse"
      via: "Zustand selector for row_count, column_count, missing_ratio, duplicate_count"
      pattern: "useStore.*analysisResult"
    - from: "frontend/src/components/analysis/QualityAlerts.tsx"
      to: "QualityAlert[]"
      via: "Zustand selector for quality_alerts"
      pattern: "useStore.*quality_alerts"
    - from: "backend/app/api/scatter.py"
      to: "backend/app/api/upload.py"
      via: "_datasets dict lookup by file_id"
      pattern: "_datasets\\.get"
---

<objective>
Build the summary/quality display components and the backend scatter downsampling endpoint. These are independent of chart rendering (Plan 01) and provide the data overview + quality alerting layer.

Purpose: Summary cards and quality alerts form the top of the EDA page layout (per user decision: summary -> quality alerts -> charts). The scatter endpoint is needed by Plan 03 for the heatmap scatter modal.

Output: 3 new frontend components, 2 Wave 0 test stubs, 1 new backend endpoint with tests.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualization/03-CONTEXT.md
@.planning/phases/03-visualization/03-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/types/analysis.ts:
```typescript
export interface SummaryStats {
  mean: number | null; std: number | null; min: number | null; max: number | null;
  q1: number | null; median: number | null; q3: number | null; skewness: number | null;
}
export interface QualityAlert {
  column: string; alert_type: string; message: string; severity: string;
}
export interface AnalysisResultResponse {
  summary: Record<string, SummaryStats>; correlation: CorrelationMatrix;
  outliers: OutlierResult[]; quality_alerts: QualityAlert[];
  row_count: number; column_count: number; missing_ratio: number; duplicate_count: number;
}
```

From backend/app/api/upload.py (dataset storage):
```python
_datasets: dict[str, pd.DataFrame] = {}
```

From backend/app/api/analyze.py:
```python
@router.post("/api/analyze", response_model=AnalysisResultResponse)
async def analyze_dataset(body: AnalyzeRequest) -> AnalysisResultResponse:
    df = _datasets.get(body.file_id)
    ...
```

From frontend/src/store/index.ts:
```typescript
export const useStore = create<StoreState>()((...args) => ({
  ...createDatasetSlice(...args), ...createAnalysisSlice(...args), ...createUiSlice(...args),
  resetStore: () => args[0](initialState),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryCard, ColumnStatsTable, QualityAlerts + Wave 0 test stubs</name>
  <files>
    frontend/src/components/analysis/SummaryCard.tsx
    frontend/src/components/analysis/ColumnStatsTable.tsx
    frontend/src/components/analysis/QualityAlerts.tsx
    frontend/src/components/analysis/__tests__/SummaryCard.test.tsx
    frontend/src/components/analysis/__tests__/QualityAlerts.test.tsx
  </files>
  <action>
    1. Create `SummaryCard.tsx`:
       - Use fine-grained Zustand selectors: `useStore(s => s.analysisResult)` — extract row_count, column_count, missing_ratio, duplicate_count
       - Render 4-column grid: `className="grid grid-cols-2 md:grid-cols-4 gap-4"`
       - Each stat card: white bg, border, rounded-lg, p-4
         - Label (text-xs text-gray-500 uppercase): "행 수", "컬럼 수", "결측 비율", "중복 행"
         - Value (text-2xl font-bold): formatted number (row_count/column_count as locale string, missing_ratio as percentage with 1 decimal, duplicate_count as locale string)
       - SUMM-03: Missing ratio is integrated here (별도 차트 불필요 per user decision) — display as percentage with color indicator (green if <5%, yellow if 5-20%, red if >20%)
       - Section header: "데이터 개요" (h2 text-lg font-semibold mb-3)
       - Wrap in React.memo

    2. Create `ColumnStatsTable.tsx` (SUMM-02):
       - Props: none (reads from store)
       - Use selector: `useStore(s => s.analysisResult?.summary)`
       - Render a table with columns: 컬럼명, 평균, 표준편차, 최소, 최대, Q1, 중앙값, Q3, 왜도
       - Each row = one numeric column from summary Record<string, SummaryStats>
       - Format numbers to 2 decimal places, show "-" for null values
       - Table styling: `w-full text-sm`, header bg-gray-50, alternating row colors
       - If no numeric columns (empty summary), show "수치형 컬럼이 없습니다." message
       - Wrap in React.memo
       - Note: For datasets with many numeric columns (>30), plain table is sufficient. Phase 2 DataTable already implements react-window for row-heavy data tables.

    3. Create `QualityAlerts.tsx` (SUMM-04):
       - Use selector: `useStore(s => s.analysisResult?.quality_alerts)`
       - If no alerts or empty array, render nothing (return null)
       - Render severity-colored banner list per user decision:
         - severity === 'warning': bg-yellow-50 border-yellow-200 text-yellow-800
         - severity === 'critical': bg-red-50 border-red-200 text-red-800
         - severity === 'info' (or default): bg-blue-50 border-blue-200 text-blue-800
       - Each banner: rounded-lg px-4 py-3, displays column name (font-medium) + message
       - Section header: "데이터 품질 알림" with count badge
       - Layout: vertical stack with gap-2
       - Wrap in React.memo

    4. Create Wave 0 test stubs:

       a. `frontend/src/components/analysis/__tests__/SummaryCard.test.tsx`:
          - Mock Zustand store with analysisResult containing row_count, column_count, missing_ratio, duplicate_count
          - Test: renders 4 stat cards with correct labels
          - Test: displays missing_ratio as percentage
          - Test: renders empty/fallback when analysisResult is null

       b. `frontend/src/components/analysis/__tests__/QualityAlerts.test.tsx`:
          - Mock Zustand store with quality_alerts array
          - Test: renders severity-colored banner for warning alert
          - Test: renders severity-colored banner for critical alert
          - Test: returns null when quality_alerts is empty
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/SummaryCard.test.tsx src/components/analysis/__tests__/QualityAlerts.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - SummaryCard renders 4-stat grid with row count, column count, missing ratio (%), duplicate count
    - Missing ratio has color-coded indicator based on threshold
    - ColumnStatsTable shows all SummaryStats fields for each numeric column
    - QualityAlerts shows severity-colored banners (yellow/red/blue) with column + message
    - SummaryCard.test.tsx passes (3 behavioral tests)
    - QualityAlerts.test.tsx passes (3 behavioral tests)
    - All three components use React.memo and fine-grained Zustand selectors
    - TypeScript compiles with zero errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create backend scatter endpoint with 2000-point downsampling</name>
  <files>
    backend/app/api/scatter.py
    backend/main.py
    backend/tests/test_scatter.py
  </files>
  <behavior>
    - Test 1: POST /api/scatter with valid file_id and two numeric column names returns 200
    - Test 2: Response contains "points" array and "total_count" integer
    - Test 3: Points array length is at most 2000
    - Test 4: Each point has "x" and "y" numeric keys
    - Test 5: Invalid file_id returns 404
    - Test 6: Non-existent column name returns 400
  </behavior>
  <action>
    1. Create `backend/app/api/scatter.py`:
       - Import _datasets from app.api.upload (same pattern as analyze.py)
       - Define ScatterRequest(BaseModel): file_id: str, col_x: str, col_y: str
       - Define ScatterResponse(BaseModel): points: list[dict[str, float]], total_count: int
       - POST /api/scatter endpoint:
         a. Look up df from _datasets[file_id], 404 if not found
         b. Validate col_x and col_y exist in df.columns, 400 if not
         c. Select the two columns, dropna() to remove rows where either is null
         d. total_count = len(filtered_df)
         e. If total_count > 2000: randomly sample 2000 rows (df.sample(n=2000, random_state=42) for reproducibility)
         f. Convert to list of {"x": float, "y": float} dicts
         g. Return ScatterResponse(points=points, total_count=total_count)

    2. Register router in `backend/main.py`:
       - Import scatter router: `from app.api.scatter import router as scatter_router`
       - Add: `app.include_router(scatter_router)`

    3. Create `backend/tests/test_scatter.py`:
       - Use same conftest.py client fixture and sample.csv fixture as existing tests
       - Helper: _upload_sample() uploads sample.csv and returns file_id
       - Test all 6 behaviors listed above
       - For downsampling test: create a larger dataset or assert len(points) <= 2000
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_scatter.py -v</automated>
  </verify>
  <done>
    - POST /api/scatter accepts file_id + col_x + col_y
    - Returns points (list of {x, y}) and total_count
    - Points capped at 2000 via random sampling with fixed seed
    - 404 for unknown file_id, 400 for invalid column names
    - All pytest tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run src/components/analysis/__tests__/ --reporter=verbose` — SummaryCard + QualityAlerts tests pass
2. `cd frontend && npx tsc --noEmit` — zero TypeScript errors
3. `cd backend && python -m pytest tests/test_scatter.py -v` — all scatter endpoint tests pass
4. `cd backend && python -m pytest` — all existing + new tests pass (no regressions)
</verification>

<success_criteria>
- SummaryCard shows 4-stat grid (row_count, column_count, missing_ratio %, duplicate_count)
- ColumnStatsTable shows per-column numeric stats with proper formatting
- QualityAlerts shows severity-colored banners (warning=yellow, critical=red, info=blue)
- SummaryCard.test.tsx and QualityAlerts.test.tsx pass with behavioral assertions
- POST /api/scatter returns at most 2000 downsampled {x,y} points
- All backend tests pass including new scatter tests
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization/03-02-SUMMARY.md`
</output>
